{
  "api_meinv_url": {
    "description": "随机小姐姐 API",
    "type": "string",
    "default": "https://v2.xxapi.cn/api/meinvpic",
    "hint": "/mm、/小姐姐、/美女、/meowpic get 默认使用这个接口。"
  },
  "api_baisi_url": {
    "description": "白丝 API",
    "type": "string",
    "default": "https://v2.xxapi.cn/api/baisi",
    "hint": "/白丝 或 /meowpic baisi 使用这个接口。"
  },
  "api_heisi_url": {
    "description": "黑丝 API",
    "type": "string",
    "default": "https://v2.xxapi.cn/api/heisi",
    "hint": "/黑丝 或 /meowpic heisi 使用这个接口。"
  },
  "api_acg_url": {
    "description": "二次元 API",
    "type": "string",
    "default": "https://v2.xxapi.cn/api/randomAcgPic?type=pc",
    "hint": "/二次元 或 /meowpic acg 使用这个接口。"
  },
  "api_jk_url": {
    "description": "JK API",
    "type": "string",
    "default": "https://v2.xxapi.cn/api/jk",
    "hint": "/jk 或 /meowpic jk 使用这个接口。"
  },
  "api_pixiv_url": {
    "description": "Pixiv 非 R18 API",
    "type": "string",
    "default": "https://api.mossia.top/duckMo?num=1&r18Type=0&proxy=i.pixiv.re",
    "hint": "/pixiv、/p站、/px 或 /meowpic pixiv 使用这个接口。默认带 r18Type=0。"
  },
  "default_api_url": {
    "description": "旧版默认 API 地址",
    "type": "string",
    "default": "",
    "hint": "兼容旧版配置。一般不用填；随机小姐姐分类会优先使用 api_meinv_url。"
  },
  "default_api_key": {
    "description": "通用 API Key（可选）",
    "type": "string",
    "default": "",
    "obvious_hint": true,
    "hint": "可选。仅当 API 地址里写了 {api_key} 占位符时才会被替换进去。"
  },
  "request_timeout_seconds": {
    "description": "请求超时（秒）",
    "type": "float",
    "default": 15.0,
    "hint": "访问图片 API 的最大等待秒数，范围 3-120。"
  },
  "image_referer": {
    "description": "图片下载 Referer",
    "type": "string",
    "default": "auto",
    "hint": "用于处理图床 HTTP 403 防盗链。auto 会自动尝试无 Referer、图片域名 Referer、API 域名 Referer；none 表示不发送 Referer；也可以填写固定地址，例如 https://example.com/"
  },
  "rate_limit_enabled": {
    "description": "启用限流",
    "type": "bool",
    "default": true,
    "hint": "开启后，同一用户在短时间内请求超过限制会被拦截。"
  },
  "rate_limit_count": {
    "description": "限流次数",
    "type": "int",
    "default": 3,
    "hint": "同一用户在限流窗口内最多可请求次数。"
  },
  "rate_limit_window_seconds": {
    "description": "限流窗口（秒）",
    "type": "float",
    "default": 60.0,
    "hint": "限流统计的时间窗口，范围 1-3600 秒。"
  },
  "rate_limit_message": {
    "description": "限流提示语",
    "type": "string",
    "default": "冲的太快了喵~",
    "hint": "请求过快时返回的提示文字。"
  }
}
